parameters:
  kapitan:
    compile:
      - input_paths:
          - harbor/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - harbor/component/main.jsonnet
        input_type: jsonnet
        output_path: harbor/

